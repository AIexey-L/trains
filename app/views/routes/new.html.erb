<h1>New Route</h1>

<%= form_for(@route) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this train from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_station %>
    <%= f.text_field :first_station %>
    <%= f.label :last_station %>
    <%= f.text_field :last_station %>
  </div>

  <div class="actions">
    <%= f.submit 'Create' %>
  </div>
  <%= link_to 'Back', routes_path %>
<% end %>
